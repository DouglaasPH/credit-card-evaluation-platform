spring.application.name=msclientes

server.port=0

eureka.client.service-url.defaultZone=http://curso-ms-eureka-user:elkfdakldlfdafd35465@${EUREKA_SERVER}:8761/eureka
eureka.instance.instance-id= ${spring.application.name}:${spring.application.instance_id:${random.value}}

management.endpoints.web.exposure.include=*
logging.file.name=./logs/log-file.log

spring.datasource.url=jdbc:mysql://${MYSQL_SERVER}:${MYSQL_PORT}/${MYSQL_DATABASE}
spring.datasource.username=${MYSQL_USER}
spring.datasource.password=${MYSQL_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect